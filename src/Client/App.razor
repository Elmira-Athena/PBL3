<MudThemeProvider @bind-IsDarkMode="_isDarkMode" Theme="_theme" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly" NotFoundPage="typeof(Pages.NotFound)">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <NotAuthorized>
                    @if (context.User.Identity?.IsAuthenticated == true)
                    {
                        <MudContainer MaxWidth="MaxWidth.Medium" Class="mt-16">
                            <MudAlert Severity="Severity.Error" Variant="Variant.Filled" Elevation="2">
                                <MudText Typo="Typo.h6">Truy cập bị từ chối!</MudText>
                                <MudText>Bạn không có quyền truy cập vào trang này.</MudText>
                            </MudAlert>
                            <MudButton Variant="Variant.Text" Color="Color.Primary" Href="/" Class="mt-2">
                                Quay về trang chủ
                            </MudButton>
                        </MudContainer>
                    }
                    else
                    {
                        <Client.Auth.RedirectToLogin />
                    }
                </NotAuthorized>
            </AuthorizeRouteView>
            <FocusOnNavigate RouteData="@routeData" Selector="h1" />
        </Found>
    </Router>
</CascadingAuthenticationState>

@code {
    private bool _isDarkMode = false;

    private MudTheme _theme = new MudTheme
    {
        PaletteLight = new PaletteLight
        {
            Primary = "#D32F2F",
            PrimaryDarken = "#B71C1C",
            PrimaryLighten = "#EF5350",
            Secondary = "#424242",
            AppbarBackground = "#FFFFFF",
            AppbarText = "#333333",
            Background = "#F5F5F5",
            Surface = "#FFFFFF",
            DrawerBackground = "#FFFFFF",
            DrawerText = "#333333",
            TextPrimary = "#333333",
            TextSecondary = "#666666",
            ActionDefault = "#757575",
            ActionDisabled = "#BDBDBD",
            Divider = "#E0E0E0",
            Success = "#43A047",
            Warning = "#FB8C00",
            Error = "#E53935",
            Info = "#1E88E5",
        },
        Typography = new Typography
        {
            Default = new DefaultTypography
            {
                FontFamily = ["Roboto", "Helvetica", "Arial", "sans-serif"],
                FontSize = "0.875rem",
            },
            H5 = new H5Typography
            {
                FontWeight = "700",
                FontSize = "1.4rem",
            },
            H6 = new H6Typography
            {
                FontWeight = "600",
                FontSize = "1.1rem",
            },
        },
        LayoutProperties = new LayoutProperties
        {
            DrawerWidthLeft = "260px",
        }
    };
}