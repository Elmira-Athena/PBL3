# KHO PROMPT MẪU CHO ANTIGRAVITY (AI CODING)

Dưới đây là các mẫu lệnh (Prompt) chuẩn để yêu cầu AI code theo kiến trúc N-Layer của dự án TechGear.
**Cách dùng:** Copy nội dung trong block code -> Thay thế các phần trong ngoặc `[...]` bằng thông tin thực tế -> Gửi cho AI.

---

## 1. PROMPT KHỞI ĐỘNG (Dùng khi bắt đầu phiên làm việc mới)
*Gửi kèm: `tech_context.md` và `database_schema.sql`*

```text
Tôi gửi kèm 2 file quan trọng:
1. `tech_context.md`: Quy tắc code, kiến trúc hệ thống và Tech Stack.
2. `database_schema.sql`: Cấu trúc Database hiện tại.

Nhiệm vụ của bạn:
- Đóng vai trò Senior .NET Full-stack Developer.
- Đọc kỹ và ghi nhớ toàn bộ ngữ cảnh trong 2 file này.
- Tuyệt đối tuân thủ Naming Convention và mô hình N-Layer đã định nghĩa.

Nếu đã hiểu rõ Context, hãy trả lời ngắn gọn: "Ready to code [Project Name]" và liệt kê 3 bảng quan trọng nhất trong DB.